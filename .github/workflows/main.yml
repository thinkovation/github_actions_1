# Step 1 V1
name: Deploy Go Program

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - run: |
        echo "A initial message"
        ls
        mkdir -p ~/.ssh
        ssh-keyscan auth.thinkovation.com > ~/.ssh/known_hosts
        ls ~/.ssh
        cat ~/.ssh/known_hosts
        echo "Another message or command"
        #python myscript.py
        #bash some-shell-script-file.sh -xe
    - run: echo "One last message"
    - name: Copy assets and binary to remote server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SSH_PORT }}
        fingerprint: ${{ secrets.SSH_FINGERPRINT }}
        script: |
         # Remove existing binary and assets folder
          rm -rf /home/msauth/msauthserver
          rm -rf /home/msauth/assets

          # Copy new binary and assets folder
          scp -r test.md assets ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/home/msauth/

    
     
